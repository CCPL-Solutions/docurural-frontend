# Borrador de reglas del proyecto — insumo para la constitución de Spec Kit

> Fecha: 2026-09-22 (estado actualizado al cerrar la Fase 3, 2026-09-23) · Base: decisiones cerradas en el checkpoint (D1–D40 según las recomendaciones de `docs/auditoria-consistencia.md`, respuestas P1–P13 y Q1–Q4 de `docs/plan-remediacion.md`).
> Alcance: Angular 21.2, zoneless. Idioma del documento: español (P4).
> Cada regla indica su **estado hoy** (✅ Cumple · 🟡 Parcial · ❌ No cumple), la evidencia, **cómo se verifica** y la **fase** de `docs/plan-remediacion.md` que la hace cumplir.
> Solo se incluyen reglas justificadas por una decisión tomada. Las reglas que el código ya cumple pero que no pasaron por el checkpoint se listan al final para ratificarlas.

---

## 1. Arquitectura y límites entre capas

| ID     | Regla                                                                                                                                                                                                                      | Estado hoy | Evidencia                                                                                                                                      | Verificación                                                             | Fase | Origen |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---- | ------ |
| ARQ-01 | `core/` contiene solo lo transversal. Un modelo o tipo que usa una sola feature vive dentro de esa feature                                                                                                                 | ✅         | Desde la Fase 3: `document-filters` vive en `features/documents/document-list/` y `toggle-status-dialog` en su diálogo                         | Revisión + script de consumidores por modelo                             | 3    | D10    |
| ARQ-02 | Una feature no importa archivos internos de otra. Lo compartido vive en `shared/` y, si es lógica de negocio, en `features/<feature>/dialogs/` como superficie pública                                                     | ✅         | Desde la Fase 3: 0 imports cruzados. `dashboard` solo importa `features/documents/dialogs/`                                                    | ESLint `@typescript-eslint/no-restricted-imports` (en `error`)           | 3    | D11    |
| ARQ-03 | Los tipos `XxxDialogData` y `XxxDialogResult` se declaran en el archivo del componente de diálogo                                                                                                                          | ✅         | Desde la Fase 3: 8 de 8 diálogos                                                                                                               | Grep `DialogData` en `core/` = 0                                         | 3    | D9     |
| ARQ-04 | Los archivos de modelo usan el sufijo `.model.ts`                                                                                                                                                                          | ✅         | Desde la Fase 2: 24 de 24                                                                                                                      | `find -name '*.models.ts'` = 0                                           | 2    | D12    |
| ARQ-05 | Los imports entre carpetas raíz usan los alias `@core/*`, `@shared/*`, `@features/*` y `@env/*`. Los imports relativos solo se usan dentro de la misma carpeta de feature o componente, sin superar dos niveles (`../../`) | ✅         | Desde la Fase 2: alias en `tsconfig.json` (`@env/*` para `src/environments`, que usan los servicios de `core/`); 0 imports con 3 o más niveles | ESLint `@typescript-eslint/no-restricted-imports` (patrón `(\.\./){3,}`) | 2    | Q3     |

## 2. Componentes y detección de cambios

| ID     | Regla                                                                                                                        | Estado hoy | Evidencia                                                                                                                                    | Verificación                                                                                    | Fase | Origen |
| ------ | ---------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ---- | ------ |
| CMP-01 | Todo componente declara `changeDetection: ChangeDetectionStrategy.OnPush`                                                    | 🟡         | 45 de 51                                                                                                                                     | ESLint `prefer-on-push-component-change-detection`                                              | 7    | D3     |
| CMP-02 | Inputs y outputs se declaran con `input()`, `input.required()` y `output()`. Se prohíben `@Input` y `@Output`                | ✅         | Desde la Fase 2: 0 decoradores                                                                                                               | ESLint `prefer-signals` y `prefer-output-emitter-ref`                                           | 2    | D1     |
| CMP-03 | Ningún output se llama como un evento DOM nativo (`submit`, `close`, `reset`, `click`…)                                      | ✅         | Desde la Fase 2: `searchSubmit`, `resetFilters` y `closePanel`                                                                               | ESLint `no-output-native`                                                                       | 2    | D2     |
| CMP-04 | La carga inicial de datos se lanza en `ngOnInit`, nunca en el constructor                                                    | 🟡         | 5 de 8                                                                                                                                       | Revisión + grep de `this.load` en constructores                                                 | 7    | D4     |
| CMP-05 | Todo `@for` usa `track` con la identidad inline (`track x.id`). Se prohíben los métodos `trackBy`                            | ✅         | Desde la Fase 2: 24 de 24                                                                                                                    | Grep `trackById` = 0                                                                            | 2    | D5     |
| CMP-06 | Las clases dinámicas se enlazan con `[class]` o `[class.x]`. Se prohíbe `NgClass`                                            | ✅         | Desde la Fase 2: 0 usos de `NgClass`                                                                                                         | ESLint `@typescript-eslint/no-restricted-imports` (`NgClass`) y `template/prefer-class-binding` | 2    | D6     |
| CMP-07 | Los estilos de componente van en `styleUrl`. Se prohíbe `styles: [...]` inline                                               | ✅         | Desde la Fase 2: 47 de 47                                                                                                                    | ESLint `no-restricted-syntax`                                                                   | 2    | D7     |
| CMP-08 | La plantilla va inline solo si el componente es presentacional y la plantilla tiene ≤ 40 líneas. En otro caso, `templateUrl` | ✅         | La plantilla inline más larga tiene 38 líneas (`document-row-actions`). _Umbral calibrado al criterio implícito existente (D8 decía "≈ 30")_ | Revisión                                                                                        | —    | D8     |
| CMP-09 | Los componentes son standalone por defecto: no se declara `standalone: true` ni se usan NgModules                            | ✅         | Desde la Fase 2: 0 `standalone: true`                                                                                                        | ESLint `no-restricted-syntax`                                                                   | 2    | Q3     |

## 3. Estado y flujo de datos

| ID     | Regla                                                                                                                                                                                   | Estado hoy | Evidencia                                                                        | Verificación                      | Fase | Origen  |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------- | --------------------------------- | ---- | ------- |
| EST-01 | Los datos se obtienen con servicio → `subscribe` → `signal.set`. `httpResource`/`rxResource` no se adoptan hasta reevaluarlo con Angular 22                                             | ✅         | 38 `subscribe`, 0 `resource`                                                     | Revisión                          | —    | D18, P6 |
| EST-02 | Toda suscripción dentro de un componente usa `takeUntilDestroyed()`                                                                                                                     | ❌         | 0 de 38                                                                          | Script o `eslint-plugin-rxjs-x`   | 7    | D19     |
| EST-03 | Las recargas repetibles (paginación, orden, búsqueda, filtros) se encadenan con `switchMap` para descartar respuestas obsoletas                                                         | ❌         | `document-list`, `user-list` y `category-list` sin cancelación                   | Test de respuestas fuera de orden | 7    | D19     |
| EST-04 | Todo recurso creado con `URL.createObjectURL` se revoca, también si el componente se destruye antes de recibir la respuesta                                                             | ❌         | `document-detail.component.ts:264-270`                                           | Test                              | 7    | D19     |
| EST-05 | `effect()` solo sincroniza con APIs imperativas (FormControl, DOM, librerías de terceros). Nunca escribe en signals para derivar estado: para eso están `computed()` y `linkedSignal()` | 🟡         | 3 de 4 `effect` son puentes; 1 propaga estado (`document-list.component.ts:257`) | Revisión (grep `effect(`)         | 7    | D20     |

## 4. Capa de datos y comunicación con el backend

| ID     | Regla                                                                                                                                | Estado hoy | Evidencia                                                       | Verificación                                                   | Fase | Origen  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------- | -------------------------------------------------------------- | ---- | ------- |
| API-01 | El cuerpo de un error HTTP se lee solo mediante `toApiError()`. Se prohíbe acceder a `err.error`                                     | ❌         | 8 accesos sin tipar y 8 casts                                   | Grep `err\.error` en `features/` = 0                           | 5    | D13     |
| API-02 | El 401 lo gestiona **exclusivamente** el interceptor. Ningún componente notifica ni ramifica por un 401                              | 🟡         | 2 de 5 pantallas lo ignoran                                     | Grep `status === 401` en `features/` = 0 + test de `httpError` | 5    | D14     |
| API-03 | Los errores de campo del backend (`fieldErrors`) se aplican con `applyFieldErrors()`, que también marca el control como `touched`    | ❌         | 5 copias, 1 sin `markAsTouched`                                 | Grep `fieldErrors` en `features/` = 0                          | 5    | D15     |
| API-04 | Un error al cargar una página se comunica con un toast (`NotificationService`) más `app-empty-state`. No hay bloques de error inline | 🟡         | 3 listas con toast; el dashboard, con error inline              | Revisión                                                       | 5    | D16, P3 |
| API-05 | Toda descarga de documento pasa por `DocumentDownloadService` (spinner por elemento, toast de éxito y de error)                      | ❌         | 3 implementaciones                                              | Grep `triggerBlobDownload` solo en el servicio                 | 5    | D17     |
| API-06 | Las señales de carga se llaman `loading` o `loadingXxx`                                                                              | ✅         | Desde la Fase 2: `loadingCategories`                            | Grep `[a-z]+Loading = signal` = 0                              | 2    | D39     |
| API-07 | Los cuerpos de las peticiones se tipan (parámetro tipado o `satisfies XxxRequest`)                                                   | ✅         | Desde la Fase 2: `{ status } satisfies UpdateUserStatusRequest` | Revisión                                                       | 2    | D40     |

## 5. Ruteo, navegación y autorización

| ID     | Regla                                                                                                                                                 | Estado hoy | Evidencia                                                                                                                      | Verificación                                                       | Fase | Origen  |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ | ---- | ------- |
| RUT-01 | La navegación que no depende de lógica previa usa `routerLink`. `router.navigate` solo se usa después de una lógica (guardar, validar, cerrar sesión) | 🟡         | Las filas del listado y del dashboard navegan por código                                                                       | Revisión                                                           | 6    | D35     |
| RUT-02 | Los permisos por rol se consultan solo con las funciones de `core/auth/permissions.ts`. Se prohíbe comparar `role === '…'` fuera de ese archivo       | ✅         | Desde la Fase 3: `isAdmin`, `isEditor`, `canSeeUploadedByFilter`, `canEditDocument`, `canDeleteDocument` y `canUploadDocument` | Grep `=== '(ADMIN\|EDITOR\|READER)'` fuera de `permissions.ts` = 0 | 3    | D33     |
| RUT-03 | Los permisos sobre un recurso comparan **ids** de usuario, nunca nombres                                                                              | ❌         | `document-permissions.ts:3-5` compara `fullName`                                                                               | Test de `canEditDocument`                                          | 6    | D34, P1 |
| RUT-04 | Toda ruta desconocida (`**`) muestra la página 404 propia                                                                                             | ❌         | `**` → `login`                                                                                                                 | Test de ruta                                                       | 6    | P8      |
| RUT-05 | Todo query param que la app escribe tiene un consumidor. No hay parámetros sin efecto                                                                 | 🟡         | `?action=upload` no lo lee nadie                                                                                               | Revisión                                                           | 6    | P2      |

## 6. Formularios y validación

| ID     | Regla                                                                                                                                                                                | Estado hoy | Evidencia                                                                                                                                   | Verificación                                                                          | Fase | Origen  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---- | ------- |
| FRM-01 | Los formularios son Reactive Forms construidos con `NonNullableFormBuilder` (`fb.nonNullable`). Se prohíben los casts en valores iniciales (`'' as string \| null`)                  | 🟡         | 3 de 6                                                                                                                                      | Grep `fb\.group(` = 0                                                                 | 5    | D21, P6 |
| FRM-02 | Los límites de validación compartidos con el backend son constantes (`MAX_*`, `MIN_*`) declaradas junto a su modelo. Se prohíben los literales en `Validators.maxLength`/`minLength` | ❌         | 11 literales, 1 constante                                                                                                                   | Grep `Validators\.(max\|min)Length\([0-9]` = 0                                        | 5    | D22     |
| FRM-03 | Los mensajes de error de campo se muestran con `<app-field-error>` y un mapa de mensajes por feature. Se prohíben los métodos `xxxError()` por componente                            | ❌         | 24 métodos en 6 archivos                                                                                                                    | Grep `Error\(\): string \| null` = 0                                                  | 5    | D23     |
| FRM-04 | Las longitudes mínimas de texto se validan sobre el valor recortado (`trimmedMinLength`)                                                                                             | 🟡         | Categoría sí, usuario no                                                                                                                    | Revisión                                                                              | 5    | D24, P7 |
| FRM-05 | Los campos sueltos fuera de un formulario se enlazan con `(input)` más un signal. Se prohíben `ngModel` y `FormsModule`                                                              | 🟡         | 1 `ngModel` (`user-list`)                                                                                                                   | ESLint `no-restricted-imports` (`FormsModule`)                                        | 5    | D25     |
| FRM-06 | Los formatos de los datepickers se proveen una única vez, en `app.config.ts` (`provideNativeDateAdapter(APP_DATE_FORMATS)`). Ningún componente provee `MAT_DATE_FORMATS`             | ✅         | Desde la Fase 3: `APP_DATE_FORMATS` (`shared/utils/date-formats.ts`) con `provideNativeDateAdapter` en `app.config.ts`; 0 providers locales | Grep `MAT_DATE_FORMATS` en `src/app` = 0 y `APP_DATE_FORMATS` solo en `app.config.ts` | 3    | D26     |

## 7. UI, estilos y sistema de diseño

| ID    | Regla                                                                                                                                                                                                                                                                     | Estado hoy     | Evidencia                                                                       | Verificación                                                | Fase                              | Origen   |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------- | -------- |
| UI-01 | Los colores se expresan solo con tokens `var(--color-*)` definidos en `src/styles/_tokens.scss`. Se prohíben los hex y `rgb()` en SCSS de componente y en TS, salvo el módulo único de paletas (y su espejo documentado para chart.js)                                    | ❌             | 48 hex en SCSS y 70 en TS                                                       | `npm run check:styles`                                      | 4                                 | D29      |
| UI-02 | Un color nuevo se añade primero como token, siguiendo `src/styles/README.md`                                                                                                                                                                                              | ❌             | Colores casi idénticos a tokens (`#c8def1` frente a `#c8ddf1`)                  | Revisión + `check:styles`                                   | 4                                 | D29      |
| UI-03 | Las media queries se escriben solo con los mixins `bp.lg`/`bp.md`/`bp.sm`. En TS se usa `BreakpointObserver` con los mismos valores. Se prohíbe `window.innerWidth`                                                                                                       | 🟡             | 15 usos del mixin, 17 `@media` crudos, 1 `window.innerWidth`                    | `npm run check:styles`                                      | 4                                 | D27      |
| UI-04 | Los parciales SCSS se importan por nombre (`@use 'breakpoints'`), nunca por ruta relativa                                                                                                                                                                                 | ✅             | Desde la Fase 2: 15 de 15                                                       | Grep `@use '\.\./` = 0                                      | 2                                 | D28      |
| UI-05 | Los botones de acción con texto usan `<app-button>`, y los de solo icono, `<app-icon-button>`. Se prohíben botones nativos que reimplementen variantes del botón                                                                                                          | 🟡             | 4 botones propios (uno se renderiza sin estilos)                                | `check:styles` (`class="btn` fuera de `button/`) + revisión | 4                                 | D30      |
| UI-06 | Las confirmaciones son diálogos a medida sobre `_confirmation-dialog.scss`. No existe un diálogo de confirmación genérico                                                                                                                                                 | ✅             | Desde la Fase 2: `ConfirmDialogComponent` eliminado                             | `find` del componente = 0                                   | 2                                 | D31, P11 |
| UI-07 | Los tamaños de diálogo se toman de las constantes de `shared/ui/dialog-sizes.ts`                                                                                                                                                                                          | ✅             | Desde la Fase 3: `DIALOG_SM`/`MD`/`LG`/`XL` en las 12 llamadas                  | Grep `width: '[0-9]` en `dialog.open` = 0                   | 3                                 | D32      |
| UI-08 | El cuerpo de texto nunca baja de `--text-base` (16 px). Los tamaños menores son solo para captions, badges y etiquetas                                                                                                                                                    | ⚪ No auditado | Regla citada en `_tokens.scss:80` y en la §UI de `CLAUDE.md`                    | Revisión visual                                             | 0 (documentación) · 4 (auditoría) | P13      |
| UI-09 | Tamaños de letra, espaciado (`padding`, `margin`, `gap`) y radios se expresan solo con tokens (`--text-*`, `--space-*`, `--radius-*`). Solo se permiten literales en las excepciones documentadas en `check:styles` (`0`, bordes de `1px`, tamaños intrínsecos de iconos) | 🟡             | Con token: 123 `font-size`, 205 espaciados y 78 radios. Literales: 91, 136 y 23 | `npm run check:styles`                                      | 4                                 | D29, Q4  |
| UI-10 | Todo `<label>` está asociado a un control (`for` o control anidado). Las etiquetas visuales de controles de Material que se asocian vía `aria-labelledby` usan `<span>` o `<div>` con `id`, no `<label>`                                                                  | ✅             | Desde la Fase 2: los 14 `<label>` son `<span>` con su `id`                      | ESLint `template/label-has-associated-control`              | 2                                 | Q5       |

## 8. Convenciones, tipado y calidad

| ID     | Regla                                                                                                                                                                                      | Estado hoy | Evidencia                                                                                                                                        | Verificación                                                          | Fase                                    | Origen  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | --------------------------------------- | ------- |
| CAL-01 | Los textos de la interfaz tratan al usuario de **usted**                                                                                                                                   | ✅         | Desde la Fase 2: 0 formas de tuteo                                                                                                               | Grep de formas de tuteo = 0                                           | 2                                       | D37     |
| CAL-02 | Todo texto visible se marca para traducción con `@angular/localize`: atributo `i18n` con ID personalizado en plantillas y `$localize` en TypeScript                                        | ❌         | 0 textos marcados                                                                                                                                | ESLint `@angular-eslint/template/i18n` + `ng extract-i18n` sin avisos | 8                                       | P5, Q1  |
| CAL-03 | Las fechas y números se formatean con los pipes de Angular (o `formatDate`) según `LOCALE_ID`. Se prohíbe `new Intl.*Format` con un locale fijo                                            | ✅         | Desde la Fase 3: pipe `date` y `formatDate` con `LOCALE_ID` `es-CO`; formatos con nombre en `shared/utils/date-formats.ts`. Corrige R11          | Grep `new Intl\.` = 0                                                 | 3                                       | D36, P5 |
| CAL-04 | Cada utilidad tiene una sola implementación en `shared/utils/` (iniciales, color por nombre, parseo de fechas, formato de archivo, descarga)                                               | ✅         | Desde la Fase 3: `user-initials`, `name-color`, `parse-date`, `document-format` (`inferFormat`) y `download-blob` en `shared/utils/`             | Revisión + grep de firmas                                             | 3                                       | D36     |
| CAL-05 | Los commits siguen Conventional Commits con descripción en **español**. El CHANGELOG, la documentación y la constitución se escriben en español                                            | 🟡         | 3 de 67 commits en español; CHANGELOG ya en español                                                                                              | Revisión (opcional: commitlint)                                       | 0                                       | P4, Q2  |
| CAL-06 | `npm run lint` (angular-eslint) pasa sin errores en la CI. Toda regla de esta constitución que sea automatizable se verifica con lint o con `check:styles`                                 | 🟡         | Desde la Fase 3: lint en la CI con 0 errores. Quedan en `warn` 6 avisos (5 `prefer-on-push`, Fase 7, y 1 `FormsModule`, Fase 5)                  | CI                                                                    | 0 → cada fase pasa sus reglas a `error` | P10     |
| CAL-07 | Servicios, guards, interceptores, validadores y utilidades tienen spec. Cobertura mínima de líneas: **80 % en `core/` y `shared/utils/`, 70 % global**, con un umbral que solo puede subir | 🟡         | Desde la Fase 3: umbral aplicado en `core/` (98,6 %) y `shared/utils/` (100 %), y trinquete global (26 % de líneas). Falta llegar al 70 % global | Umbral de cobertura en `npm run test:ci`                              | 1 (el umbral sube hasta el objetivo)    | P12     |
| CAL-08 | Los flujos críticos (login, listado con búsqueda, detalle, subida) tienen un E2E de humo con la API simulada                                                                               | ✅         | Desde la Fase 1: 8 E2E de flujos, ejecutados en la CI (`npm run e2e:ci`), y 12 capturas de referencia solo en local (`npm run e2e`)              | CI (job `e2e`)                                                        | 1                                       | P12     |
| CAL-09 | La CI se ejecuta en `push` a `feature/**`, `bugfix/**` y `hotfix/**`, no en PR, con Prettier, lint, tests con umbral, build de producción y E2E de flujos                                  | ✅         | Desde la Fase 1: Prettier, ESLint, `check:styles`, tests con umbral de cobertura, build y, en paralelo, E2E de flujos                            | `ci.yml`                                                              | 0 · 1                                   | P9, P10 |
| CAL-10 | Existe un `CLAUDE.md` en la raíz, actualizado, que remite a esta constitución y contiene la sección §UI                                                                                    | ✅         | Creado en la Fase 0                                                                                                                              | Revisión                                                              | 0                                       | P13     |
| CAL-11 | Todo el código (clases, métodos, variables, archivos, carpetas, rutas y clases CSS) usa identificadores en **inglés**                                                                      | ✅         | Desde la Fase 2: `campo` → `field`                                                                                                               | Revisión                                                              | 2                                       | Q2      |

---

## Reglas candidatas que ya se cumplen pero no pasaron por el checkpoint

El código ya las cumple de forma uniforme, pero no se decidieron explícitamente. **No se incluyen en la constitución hasta que las ratifiques.**

| Candidata                                                                                                       | Estado hoy                               |
| --------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| Inyección de dependencias solo con `inject()`                                                                   | ✅ 85 de 85                              |
| Control de flujo `@if`/`@for`/`@switch`. Se prohíben `*ngIf`/`*ngFor`                                           | ✅ 0 directivas estructurales            |
| Prefijo `app-` en todos los selectores                                                                          | ✅ 51/51                                 |
| `strict` y `strictTemplates` activos, 0 `any` explícitos                                                        | ✅                                       |
| El HTTP vive solo en `core/services`, con un servicio por recurso y URL construida con `environment.apiBaseUrl` | ✅ 20/20                                 |
| Todas las rutas son lazy (`loadComponent`) y tienen `title`                                                     | ✅ 7/7                                   |
| Guards e interceptores funcionales                                                                              | ✅ 4/4                                   |
| Comentarios de código en español                                                                                | ✅ 279 de 282 líneas                     |
| `interface` para formas de datos y `type` para uniones                                                          | ✅ 48 / 16                               |
| Un único mecanismo de toasts (`NotificationService`)                                                            | ✅                                       |
| La sesión se valida en cada navegación (`canActivateChild`) y caduca con `expiresAt`                            | ❌ (lo corrige la Fase 6 como riesgo R4) |
